import type { Metadata } from 'next'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

export const metadata: Metadata = {
  title: 'KI Agentur - The AI Capabilities Your Competitors Wish They Had',
  description: 'Premium AI engineering using n8n, Claude Code, MCP & Docker. Cutting-edge automation for tech-forward companies.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
